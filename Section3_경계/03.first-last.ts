{
  /**
   * first-last
   * 포함된 양의 시작부터 끝까지 의미
   */

  const students: string[] = ['포코', '존', '현석'];

  function getStudents(first: string, last: string) {
    // ... some code
  }

  getStudents(students[0], students[2]);
}
